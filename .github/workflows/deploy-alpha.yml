name: Alpha 배포

on:
  workflow_dispatch:
    inputs:
      deployment_phase:
        description: '앱 배포 단계 (기본값: alpha). alpha, beta, production 중 선택. 이외의 값은 허용되지 않음.'
        required: false
        type: string
        default: 'alpha'
      image_name:
        description: 'Docker 이미지 이름 (기본값: repository 이름)'
        required: false
        type: string
      health_check_path:
        description: 'Health check endpoint 경로 (기본값: /api/health)'
        required: false
        type: string
        default: '/api/health'
      ecr_base_name:
        description: 'ECR 기본 경로 (기본값: aws-deploy-wizard)'
        required: false
        type: string
        default: 'aws-deploy-wizard'
      ecs_cluster_base_name:
        description: 'ECS Cluster 이름 (기본값: aws-deploy-wizard)'
        required: false
        type: string
        default: 'aws-deploy-wizard'

jobs:
  ## Docker image 빌드: 애플리케이션을 패키징합니다.
  build-docker-image:
    runs-on: ubuntu-latest
    environment: alpha
    permissions:
      id-token: write
      contents: read
    outputs:
      image_name: ${{ steps.set-image-name.outputs.image_name }}
      image_tag: ${{ steps.set-image-name.outputs.image_tag }}

    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v4

      - name: 애플리케이션 배포 단계 검증
        run: |
          if [ ${{ inputs.deployment_phase }} != "alpha" ] && [ ${{ inputs.deployment_phase }} != "beta" ] && [ ${{ inputs.deployment_phase }} != "production" ]; then
            echo "❌ 오류: 잘못된 deployment_phase 값입니다. 허용되는 값은 'alpha', 'beta', 'production' 입니다."
            exit 1
          fi

      - name: 이미지 이름 및 태그 설정
        id: set-image-name
        run: |
          # Input으로 이미지 이름이 제공되었으면 사용, 아니면 repository 이름 사용
          if [ -n "${{ inputs.image_name }}" ]; then
            IMAGE_NAME="${{ inputs.image_name }}"
          else
            IMAGE_NAME="${{ github.event.repository.name }}"
          fi

          # 태그는 git commit SHA의 짧은 버전 사용
          IMAGE_TAG="${{ github.sha }}"
          IMAGE_TAG_SHORT="${IMAGE_TAG:0:7}"

          echo "image_name=${IMAGE_NAME}" >> $GITHUB_OUTPUT
          echo "image_tag=${IMAGE_TAG_SHORT}" >> $GITHUB_OUTPUT

          echo "📦 Docker 이미지 이름: ${IMAGE_NAME}"
          echo "🏷️  Docker 이미지 태그: ${IMAGE_TAG_SHORT}"

      - name: Docker image 생성(애플리케이션 패키징)   # Target 배포환경에 따라 아키텍쳐 명시 필요할 수도 있음
        run: |
          IMAGE_NAME="${{ steps.set-image-name.outputs.image_name }}"
          IMAGE_TAG="${{ steps.set-image-name.outputs.image_tag }}"

          echo "🔨 Docker 이미지 빌드 시작..."
          echo "이미지: ${IMAGE_NAME}:${IMAGE_TAG}"

          # Docker 이미지 빌드 (루트 디렉토리의 Dockerfile 사용)
          docker build \
            --tag "${IMAGE_NAME}:${IMAGE_TAG}" \
            --tag "${IMAGE_NAME}:latest" \
            --file Dockerfile \
            .

          echo "✅ Docker 이미지 빌드 완료"

      - name: 생성한 Docker image 검증
        run: |
          IMAGE_NAME="${{ steps.set-image-name.outputs.image_name }}"
          IMAGE_TAG="${{ steps.set-image-name.outputs.image_tag }}"

          echo "🔍 Docker 이미지 검증 중..."

          # 이미지 존재 확인
          if ! docker images "${IMAGE_NAME}:${IMAGE_TAG}" --format "{{.Repository}}:{{.Tag}}" | grep -q "${IMAGE_NAME}:${IMAGE_TAG}"; then
            echo "❌ 오류: Docker 이미지를 찾을 수 없습니다 (${IMAGE_NAME}:${IMAGE_TAG})"
            echo ""
            echo "사용 가능한 이미지 목록:"
            docker images "${IMAGE_NAME}" --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}\t{{.CreatedAt}}" || true
            exit 1
          fi

          echo "✓ 이미지 존재 확인 완료"

          # 이미지 상세 정보 확인
          echo ""
          echo "=== Docker 이미지 정보 ==="
          docker images "${IMAGE_NAME}" --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}\t{{.CreatedAt}}"

          echo ""
          echo "=== 이미지 메타데이터 ==="
          docker inspect "${IMAGE_NAME}:${IMAGE_TAG}" --format='
          - 생성 시간: {{.Created}}
          - 아키텍처: {{.Architecture}}
          - OS: {{.Os}}
          - 크기: {{.Size}} bytes
          - Exposed Ports: {{.Config.ExposedPorts}}
          - Entrypoint: {{.Config.Entrypoint}}
          - Cmd: {{.Config.Cmd}}'

          echo ""
          echo "✅ Docker 이미지 검증 완료"

  ## AWS Infrastructure 배포: 필요한 인프라를 설정합니다. 중복 배포된 인프라가 있다면, 건너뜁니다.
  deploy-infrastructure-if-necessary:
    needs: build-docker-image
    runs-on: ubuntu-latest
    environment: alpha
    permissions:
      id-token: write
      contents: read

    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v4

      - name: AWS 로그인 정보 정의
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      - name: ECR 생성    # 이미 존재하면 건너뜁니다
        run: |
          IMAGE_NAME="${{ needs.build-docker-image.outputs.image_name }}"
          REPOSITORY_NAME="${{ inputs.ecr_base_name }}/${IMAGE_NAME}"

          echo "=== Elastic Container Repository 확인 ==="
          echo "Repository 이름: ${REPOSITORY_NAME}"
          echo ""

          # 리포지토리 존재 확인
          if aws ecr describe-repositories --repository-names "${REPOSITORY_NAME}" --region ap-northeast-2 >/dev/null 2>&1; then
            echo "✓ 기존 ECR 사용: ${REPOSITORY_NAME}"
            REPOSITORY_URI=$(aws ecr describe-repositories --repository-names "${REPOSITORY_NAME}" --region ap-northeast-2 --query 'repositories[0].repositoryUri' --output text)
            echo "  URI: ${REPOSITORY_URI}"
          else
            # 리포지토리 생성
            echo "ECR 생성 중: ${REPOSITORY_NAME}"
            REPOSITORY_URI=$(aws ecr create-repository \
              --repository-name "${REPOSITORY_NAME}" \
              --region ap-northeast-2 \
              --image-scanning-configuration scanOnPush=true \
              --image-tag-mutability MUTABLE \
              --query 'repository.repositoryUri' \
              --output text)
            echo "✓ ECR 생성 완료: ${REPOSITORY_URI}"
          fi

          echo ""
          echo "REPOSITORY_URI=${REPOSITORY_URI}" >> $GITHUB_ENV

      - name: 빌드한 Docker 이미지 푸시
        run: |
          IMAGE_NAME="${{ needs.build-docker-image.outputs.image_name }}"
          IMAGE_TAG="${{ needs.build-docker-image.outputs.image_tag }}"
          REPOSITORY_URI="${{ env.REPOSITORY_URI }}"

          echo "=== Docker 이미지 빌드 및 푸시 ==="
          echo "ECR URI: ${REPOSITORY_URI}"
          echo "이미지 태그: ${IMAGE_TAG}, latest"
          echo ""

          # ECR 로그인
          echo "ECR 인증 중..."
          aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin ${REPOSITORY_URI%%/*}

          # Docker 이미지 빌드 (linux/amd64 플랫폼 - ECS Fargate 요구사항)
          echo "Docker 이미지 빌드 중 (linux/amd64)..."
          docker build --platform linux/amd64 \
            -t ${REPOSITORY_URI}:${IMAGE_TAG} \
            -t ${REPOSITORY_URI}:latest \
            -f Dockerfile \
            .

          # ECR에 푸시
          echo ""
          echo "ECR에 푸시 중..."
          docker push ${REPOSITORY_URI}:${IMAGE_TAG}
          docker push ${REPOSITORY_URI}:latest

          echo ""
          echo "✓ Docker 이미지 푸시 완료"
          echo "  - ${REPOSITORY_URI}:${IMAGE_TAG}"
          echo "  - ${REPOSITORY_URI}:latest"

      - name: ECS Cluster 생성
        run: |
          CLUSTER_NAME="${{ inputs.ecs_cluster_base_name }}"

          echo "=== ECS Cluster 확인 ==="
          echo "Cluster 이름: ${CLUSTER_NAME}"
          echo ""

          # Cluster 존재 확인
          CLUSTER_STATUS=$(aws ecs describe-clusters \
            --clusters "${CLUSTER_NAME}" \
            --region ap-northeast-2 \
            --query 'clusters[0].status' \
            --output text 2>/dev/null || echo "NONE")

          if [ "$CLUSTER_STATUS" = "ACTIVE" ]; then
            echo "✓ 기존 ECS Cluster 사용: ${CLUSTER_NAME}"
          elif [ "$CLUSTER_STATUS" = "NONE" ] || [ "$CLUSTER_STATUS" = "INACTIVE" ]; then
            # Cluster 생성
            echo "ECS Cluster 생성 중: ${CLUSTER_NAME}"
            aws ecs create-cluster \
              --cluster-name "${CLUSTER_NAME}" \
              --region ap-northeast-2 \
              --tags key=Name,value=${CLUSTER_NAME} key=ManagedBy,value=github-actions \
              > /dev/null

            echo "✓ ECS Cluster 생성 완료: ${CLUSTER_NAME}"
          else
            echo "⚠️  Cluster 상태 확인 불가: ${CLUSTER_STATUS}"
          fi

          echo ""
          echo "CLUSTER_NAME=${CLUSTER_NAME}" >> $GITHUB_ENV

      - name: ECS Task Definition 생성 (이미 존재하면 건너뜁니다)
        run: |
          echo "work in progress"

      - name: ALB 및 Target Group 생성 (이미 존재하면 건너뜁니다)
        run: |
          echo "work in progress"

      - name: ECS Service 배포 # Alpha/Beta: (Fargate Spot), Production: (Fargate On-Demand)
        run: |
          echo "work in progress"

      - name: ACM 인증서 생성 # Alpha/Beta - `*.{app_name}.ig-pilot.com`, Production: `*.{app_name}.ig-pilot.com`
        run: |
          echo "work in progress"

      - name: Route 53 DNS 레코드 생성 (ig-pilot.com 호스팅 존에 레코드 추가)
        run: |
          echo "work in progress"

  ## 배포 모니터링: 배포된 애플리케이션의 상태를 확인합니다.
  monitor-deployment:
    needs: deploy-infrastructure-if-necessary
    runs-on: ubuntu-latest
    environment: alpha
    permissions:
      id-token: write
      contents: read
    steps:
      - name: ECS 서비스 상태 확인
        run: |
          echo "work in progress"

      - name: 애플리케이션 정상 배포 여부 확인
        run: |
          echo "work in progress"

      - name: 배포 완료 알림
        run: |
          echo "work in progress"
